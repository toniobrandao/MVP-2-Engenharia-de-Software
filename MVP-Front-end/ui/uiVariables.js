// Selecionando as variáveis da UI.
const predictionButton = document.querySelector(".btn.btn-primary");
const inputs = document.querySelectorAll(".naked_table input");
const diagnosticElement = document.getElementById("diagnostic");
const h2Element = diagnosticElement.querySelector("h2");

export { predictionButton, inputs, diagnosticElement, h2Element };
